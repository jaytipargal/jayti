# Development / test dependencies for the EKA Agent test suite.
#
# The suite is designed to run fully offline: it never touches the network,
# a real database, a GPU, or the heavy ML stack. Heavy/optional runtime deps
# (faiss, sentence-transformers, chromadb, torch, transformers, peft,
# datasets, psycopg2) are stubbed in tests/conftest.py, so they are NOT
# required to run the tests.
pytest>=8.0
pytest-cov>=5.0
fastapi>=0.100
pydantic>=2.0
httpx>=0.27          # required by fastapi.testclient.TestClient
numpy>=1.24          # genuinely exercised by the FAISS offset-index logic
